x-service-defaults: &service-defaults
  user: root
  environment:
    TZ: Asia/Seoul
    SPRING_CONFIG_IMPORT: configserver:http://config:8888
  networks:
    - default
    - grafana_default
  deploy:
    replicas: 1
    update_config:
      parallelism: 1
      order: stop-first
      failure_action: rollback
    rollback_config:
      parallelism: 1
      order: stop-first
    restart_policy:
      condition: on-failure
      delay: 5s

services:
  config:
    <<: *service-defaults
    image: ghcr.io/now-start/config:latest
    environment:
      TZ: Asia/Seoul
      ENCRYPT_KEY: ${SPRING_ENCRYPT_KEY:?Set SPRING_ENCRYPT_KEY in Arcane}
      EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: http://eureka:8761/eureka/

  eureka:
    <<: *service-defaults
    image: ghcr.io/now-start/eureka:latest

  admin:
    <<: *service-defaults
    image: ghcr.io/now-start/admin:latest

  gateway:
    <<: *service-defaults
    image: ghcr.io/now-start/gateway:latest
    ports:
      - target: 8000
        published: 8000
        protocol: tcp
        mode: ingress
    environment:
      TZ: Asia/Seoul
      JAVA_TOOL_OPTIONS: -XX:MaxDirectMemorySize=256m
      SPRING_CONFIG_IMPORT: configserver:http://config:8888

networks:
  default:
    driver: overlay
  grafana_default:
    external: true
